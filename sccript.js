const = greetings => {
  console.log(`Hello word to, ${name}!`);
};
greetings('simon juma');

//The structure of writing arrow functions
const myFunction = (param1, param2) => {
  //body function,
}

//single line arrow functions
const greeting = (name) => console.log(`Hello, ${name`}!`);
/*When using arrow function with more than one line,
the curly brackets are required only when your function is 
more that a sinle line */
const greetings = () => {
   console.log('Hello juma');
   console.log("How are ypu doing today");
};

